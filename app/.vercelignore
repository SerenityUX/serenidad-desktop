# Electron-only assets — never deployed to web.
main/
release/
build/entitlements.mac.plist
entitlements.plist
Info.plist
KodanFlower.icns
KodanFlower-dock.png
# NOTE: do NOT ignore KodanFlower.png — public/KodanFlower.png is the
# banner logo on the web build. Only the repo-root icns/dock-png are
# Electron-specific and safe to drop.

# Build outputs and deps Vercel rebuilds anyway.
dist/
node_modules/
