/**
 * Shared font stack used across CoCreate. Matches TitleBar / App.js so the
 * editor, modals, and overlays all render in the same family.
 */
export const APP_FONT_STACK =
  '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif';
