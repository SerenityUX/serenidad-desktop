# Dependencies
node_modules/

# Environment & secrets — never commit real credentials
.env
.env.*
!.env.example
!api/.env.example

# Keys / certs
*.pem
*.p12
*.key

# OS / editor noise
.DS_Store
Thumbs.db
.idea/

# Logs
*.log
npm-debug.log*
npm-error.log*

# API build output (if added later)
api/dist/
